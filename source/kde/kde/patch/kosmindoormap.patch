# This isn't compatible with a newer protobuf, but try again with Plasma 6.
cat $CWD/patch/kosmindoormap/skip-protobuf.patch | patch -p1 --verbose || { touch ${SLACK_KDE_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
